# Core AI 辨識／轉寫依賴（專案根 core/）
# 從專案根執行 pipeline 時可：pip install -r core/requirements.txt
# 或與 backend 一起裝：pip install -r requirements.txt

python-dotenv
pydub
OpenCC
torch
torchaudio
torchvision
faster-whisper
pyannote-audio
openai
instructor
pydantic
